export interface AppState {
    isLoading: boolean;
    version: String;
    githubUrl: String;
}

export interface LoadResult {
    version: String;
    githubUrl: String;    
}

export interface Settings {
    
}